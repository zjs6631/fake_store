extends layout

block content
  h1= title
  p Welcome to #{title}

  a(href='./inventory') All
  ul 
        
    each category in category_list 
      li 
        a(href=category.name) #{category.name}
    
    else  
      li There are no more categories

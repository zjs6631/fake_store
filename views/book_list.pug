extends layout

block content

    h1= title

    
    each item in book_list 

        div(class='itemDivs')
            p= item.name
            p= item.price
            a(type='button' href=item.url) View item
                    //#{item.description}
                    
        
        
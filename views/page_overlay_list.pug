extends layout



block content

    

    div(class='itemDivContainer')
        each item in overlay_list 

            div(class='itemDivs')
                p= item.name
                p= "$" + (item.price).toFixed(2)
                a(type='button' href=item.url) 
                    button( type='button') View item
                    //#{item.description}
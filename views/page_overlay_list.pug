extends layout

block content

    h1= title

    ul 
        each item in overlay_list 
            li 
                a(href=item.url) #{item.name}
                    //#{item.description}
                    
        
        else
            li There are no more items